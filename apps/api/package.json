{
	"name": "crescendai-api",
	"private": true,
	"scripts": {
		"dev": "wrangler dev",
		"test": "vitest",
		"typecheck": "tsc --noEmit",
		"generate": "drizzle-kit generate",
		"migrate": "drizzle-kit migrate",
		"migrate-d1": "bun run scripts/migrate-d1.ts",
		"codegen:catalog": "bun scripts/catalog-codegen.ts",
		"test:scripts": "vitest --config vitest.node.config.ts",
		"prebuild": "bun run codegen:catalog",
		"predeploy": "bun run codegen:catalog",
		"prepare": "bun run codegen:catalog"
	},
	"dependencies": {
		"hono": "^4",
		"@hono/zod-validator": "^0.4",
		"zod": "^3",
		"better-auth": "^1",
		"drizzle-orm": "^0.38",
		"postgres": "^3",
		"@sentry/cloudflare": "^9"
	},
	"devDependencies": {
		"wrangler": "^4",
		"@cloudflare/vitest-pool-workers": "^0.8",
		"@cloudflare/workers-types": "^4",
		"vitest": "^3",
		"drizzle-kit": "^0.30",
		"typescript": "^5"
	}
}
