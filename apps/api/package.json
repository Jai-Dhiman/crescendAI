{
	"name": "crescendai-api",
	"private": true,
	"scripts": {
		"dev": "wrangler dev",
		"test": "vitest",
		"typecheck": "tsc --noEmit",
		"lint": "biome check src/",
		"lint:fix": "biome check --write src/",
		"generate": "drizzle-kit generate",
		"migrate": "drizzle-kit migrate",
		"migrate-d1": "bun run scripts/migrate-d1.ts",
		"codegen:catalog": "bun scripts/catalog-codegen.ts",
		"test:scripts": "vitest --config vitest.node.config.ts",
		"build:wasm": "cd src/wasm/score-analysis && wasm-pack build --target bundler --release && cd ../piece-identify && wasm-pack build --target bundler --release && cd ../../.. && bun run scripts/patch-wasm-shims.ts",
		"prebuild": "bun run codegen:catalog && bun run build:wasm",
		"predeploy": "bun run codegen:catalog && bun run build:wasm",
		"deploy": "wrangler deploy"
	},
	"dependencies": {
		"@hono/zod-validator": "^0.4",
		"@sentry/cloudflare": "^9",
		"better-auth": "^1",
		"drizzle-orm": "^0.38",
		"hono": "^4",
		"postgres": "^3",
		"zod": "^3",
		"zod-to-json-schema": "^3.25.2"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.6",
		"wrangler": "^4",
		"@cloudflare/vitest-pool-workers": "^0.8",
		"@cloudflare/workers-types": "^4",
		"vitest": "^3",
		"drizzle-kit": "^0.30",
		"typescript": "^5"
	}
}
