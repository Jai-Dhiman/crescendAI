# Implementation Notes

Decisions, deviations, and tradeoffs made during build. Read this before running /review.

## Task 1: Repo scaffold
- Nested standalone repo initialized by the controller via Bash (`git init -b build/initial-system`) before dispatch, per the plan's hook workaround.
- Deviation: `uv.lock` (auto-generated by `uv run`) committed alongside the scaffold — legitimate reproducibility artifact, not gitignored.
- Commit fe76710. Spec review PASS; quality review APPROVED (3 MINOR observations, all plan-specified content).

## Tasks 2-5 (Group A, parallel): schema, FeatureStore, budget guard, ratchet
- All four implemented verbatim from plan, watched-fail TDD confirmed, no deviations.
- Commits: e0d224f (schema), ad00d3b (store), b7e8d2d (budget), 745172a (ratchet).
- Spec reviews: 4x PASS. Quality review (consolidated over fe76710..745172a): all APPROVED; 3 MINOR notes — store.py continuation-indent cosmetic; verify() checks presence not dim (in-scope contract); confidence range unvalidated (not specified). No action taken.
- evalx/__init__.py race handled cleanly: budget task created it, ratchet task left it alone.

## Tasks 6-8 (Group B, parallel): folds, fixtures, encoders
- All verbatim from plan, watched-fail TDD confirmed. Commits: 1cee5ba (folds), b650992 (fixtures), 6dba076 (encoders).
- Task 6 spec compliance verified by controller directly (byte-identical diff vs plan, 3 tests pass) because the dispatched reviewer's report notification was delayed; Tasks 7/8 spec reviews PASS.
- Quality review: all APPROVED. MINOR notes: folds k>len(rest_gens) empty-group edge undocumented; dead ClapModel import in _clap_embed; _mert_text_embed reuses UnknownEncoderError for a no-text-tower condition. No action taken (untested real-encoder branch + accepted design).

## Tasks 16-18 (Group E, parallel): cmi_pref, aime_survey, music_arena adapters
- All verbatim from plan, watched-fail TDD confirmed. Commits: f6049e7 (cmi_pref), 3a80f21 (aime_survey), ea0db3e (music_arena).
- Spec review: PASS x3 (consolidated reviewer, per-task verification incl. jsonl fixture spot-check). Quality: APPROVED x3.
- MINOR notes for follow-up: aime modality="audio" hardcoded (inconsistent with other adapters, nothing consumes modality yet); aime pair_id=f"aime-{i}" is index-based and weaker than uuid-based schemes if load() is ever called per-question-type and concatenated.

## Task 9 (Group C): pointwise BT head
- Verbatim (controller byte-diff vs plan: identical), watched-fail confirmed, commit ef280b1. Test passes.
- Controller performed spec verification directly by byte-diff for sequential Group C tasks (plan specifies exact code, diff-check is exact); consolidated quality review at group end.

## Task 10: joint-head antisymmetry pin
- Tests appended verbatim (617c0ee); falsification check performed (joint=0 makes cross-clip test fail, restored to green). Controller verified commit scope + 3 tests pass.

## Task 11 + PLAN BUG FIX: trainer determinism / degenerate folds
- Task 11 (43fa4f8): plan's verbatim assemble() crashed — with 3 rotating generators and k_folds=2, make_folds' stride slicing held out 2 of 3 generators in one fold, leaving a structurally empty train set (seed-independent). Implementer initially patched assemble() with an empty-tensor fallback (_manifest_dim).
- Controller rejected the workaround (hides the degenerate fold; would sink Task 12's >0.9 smoke gate; violates explicit-exceptions norm) and dispatched a root-cause fix (dc8071e): make_folds now caps held-group size at max(1, len(rest_gens)-2) so every rotating fold retains >=2 trainable generators; regression test test_no_rotating_fold_has_empty_train added (watched fail first); assemble() reverted to plan-verbatim loud-failure form.
- Full suite after fix: 42 passed. This is a DELIBERATE deviation from the plan's specified folds.py code — flag for /review.

## Task 12 (smoke gate) + Group C quality review + folds guards
- Task 12 (98965dc): verbatim, watched-fail, passed at plan-default 150 epochs (no tuning needed) — validates the dc8071e folds fix end-to-end. just smoke green.
- Group C quality review: Tasks 9/10/11/12 APPROVED. Fix-commit dc8071e NEEDS_FIXES: [CRITICAL] k>len(rest_gens) gave empty val groups (cryptic np.stack crash); [IMPORTANT] exactly 2 rotating generators gives structurally empty train.
- Follow-up fix db3f307: explicit ValueError guards in make_folds (len(rest_gens)<3; k>len(rest_gens)) + 2 watched-fail regression tests + comment on the truncated-generator coverage tradeoff. Full suite 48 passed.

## Task 13: generator-ID probe
- Verbatim (d6b5bec), watched-fail confirmed, 2 tests pass. Note: probe tests take ~65s (sklearn LogisticRegression on 400x16).

## Task 14: decompose
- Verbatim (55c59ad), watched-fail confirmed.

## Task 15: mitigation (sampler + adversarial)
- af8ac01. Watched-fail (KeyError sample_stats). DEVIATION: plan collected sample_stats from rotating-fold fits; fold topology (one generator trainable in both folds) structurally doubles its exposure (ratio 2.13 > 1.5 test bound) even with exact within-fold balance. Implementer moved stats collection to the final_train fit (all rotating generators in one pass) — measures the sampler's balancing property directly. adv_loss unchanged (last rotating fold). Also .detach() on adv-loss accumulation (warning fix). Full suite 52 passed.
- Accepted limitation per plan/challenge: adversarial_lambda>0 + joint=1 silently no-ops (documented in Task 21 README).

## Tasks 19-20 (Group F) + Group D review fixes
- Task 19 (858d018): verbatim + challenge-mandated AIME segment tests (glob-by-track-id, HH:MM:SS slicing verified sample-exact against toy embed of the expected slice; missing-track FileNotFoundError). Suite 56.
- Task 20 (18ee407): verbatim MIREX contract entrypoint; 3 tests; deterministic output verified byte-identical across runs.
- Group D quality review: Task 13 APPROVED; Task 14/15 flagged. Controller rulings:
  (a) ACCEPTED AS DESIGN: trainer's single multinomial-with-replacement sampling path (plan-specified; uniform path uses ones-weights; bootstrap sampling is standard SGD; smoke gate passes) — flag for /review.
  (b) FIXED (4d38993): decompose now threads pairs= into the in-dist _fit so mitigation applies to BOTH regimes; regression test with tuned params (head_width=4, epochs=5, lambda=10.0) shows pre-fix in_dist 1.0==1.0 spurious equality vs post-fix mitigation engaged; suite 60 passed.
  (c) MINOR accepted: penultimate() assumes head_depth>=1 (no config uses 0); _pair_weights harmonic heuristic bound-tested at <1.5.

## Task 21 (Group G): crescendai wiring + wave-1 config
- Nested repo ef86c90: cli.py (final_generators-empty guard verified to refuse BEFORE any network call), configs/first.json, full justfile, README replaced. README includes controller-approved addition documenting the adversarial_lambda/joint=1 no-op restriction (challenge-review mitigation).
- Crescendai worktree 4237792f: .gitignore ignores mirex-trackb/; docs/mirex/ copied from issue-105 branch + decision-log entry appended.
- Deviation note: plan's Step-3 `git check-ignore mirex-trackb` (bare name) exits 1 in the worktree because the dir doesn't exist there and directory-only patterns need lstat evidence — verified rule correctness via trailing-slash form, temp-dir creation, and node_modules/ control. Rule works at the repo root where mirex-trackb/ exists.
- Group F quality review: Tasks 19, 20, fix 4d38993 all APPROVED (4 MINOR notes: glob multi-match picks sorted-first silently; lazy torch import style; TOCTOU note; corpus choices accepted).

## Final integration review + fixes
- Final whole-repo reviewer: NEEDS_FIXES with 3 IMPORTANT seam findings (no CRITICAL): (1) trainer writes checkpoints/latest/checkpoint.pt but main.py defaults checkpoints/best.pt with no producer; (2) musicarena: clip refs fell through to a misleading FileNotFoundError; (3) probe/decompose/budget headline modules had no documented entrypoint.
- Fixed in 918654f: promote-checkpoint just recipe + README wave-1 step 5 (kept main.py best.pt default — deliberate operator promotion, mirrors eval-promote); NotImplementedError guard for musicarena refs (watched-fail regression test); README "Shortcut study" + "Public-eval budget" sections.
- Controller verified all three fixes directly (guard at run.py:36, recipe present, README sections present); final suite 61 passed; just --list shows 7 recipes.
- BUILD COMPLETE: 21/21 tasks; nested repo 25 commits on build/initial-system; crescendai worktree carries Task 21 docs/gitignore + notes/plan bookkeeping.
