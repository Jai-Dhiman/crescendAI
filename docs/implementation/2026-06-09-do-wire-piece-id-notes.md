# Implementation Notes

Decisions, deviations, and tradeoffs made during build. Read this before running /review.

## Baseline (pre-build)
- typecheck: 18 pre-existing errors (11 src/harness/*, 7 unrelated test/script type errors). None in session-brain*, wasm-bridge*, or piece-identify.
- cargo test (piece-identify): 41 pass, 1 fail = `real_recording_test::identify_real_recording` (env NOTES_JSON not set) — DOCUMENTED pre-existing; will disappear after Task 5 deletes real_recording_test.rs.
- session-brain.schema.test.ts: 3 pass. wasm-bridge.test.ts: 4 pass. wasm-bridge.workerd.test.ts: 7 pass.

## Build-process deviation
- The subagent-dispatch (Task) tool is unavailable in this environment, so the controller could not dispatch fresh implementer/reviewer subagents. The build proceeded with the controller acting as implementer directly, preserving strict TDD (write test -> watch fail -> implement -> watch pass -> commit) and a two-stage self-review (spec compliance, then code quality) per task. Same discipline, single context.

## Carry-forward cautions from /challenge (PROCEED_WITH_CAUTION)
1. accumulateAndIdentify MUST mutate caller state in place; NO readState/writeState/CAS of its own.
2. MAX_IDENTIFICATION_BUFFER=1200 keeps MOST RECENT notes (slice(-CAP)); add a truncation unit test.
3. Task 3 falsification probe: set PIECE_ID_MARGIN_THRESHOLD=-1, confirm ambiguous test FAILS, restore 0.0935, record in commit body.
4. chunk_ready (finalizeChunk) wiring ships covered only indirectly; ensure helper called identically from both sites.

## Task 1 (SHA 1c5f71fb)
- Replaced identificationNoteCount with identificationNoteBuffer (array of {pitch,onset,offset,velocity}) in sessionStateSchema + createInitialState. 6/6 schema tests pass.
- KNOWN TRANSIENT: Task 1 in isolation introduces 3 typecheck errors in session-brain.ts L939-947 (legacy finalizeChunk still reads identificationNoteCount). This is expected per the plan's sequencing (Tasks 1+2 land together; Task 2 rewrites that block). The Task 1 commit scope is schema-only and its unit test passes under vitest per-file transpilation. Full-project typecheck is restored by Task 2.

## Task 2 (SHA 6a18a081)
- tryIdentifyPiece rewritten to v2 (single fingerprint/v2/piece_index.json fetch, wasm.identifyPiece, threshold 0.0935, method "identify_v2", confidence = margin).
- accumulateAndIdentify helper: mutate-in-place ONLY (caution #1 honored — zero readState/writeState/CAS inside it). Called identically from finalizeChunk and handleEvalChunk (caution #4).
- Buffer cap 1200 via slice(-CAP) keeps most-recent notes. Added a dedicated truncation test (caution #2): pushes 1300 notes, asserts buffer length 1200 with oldest retained = the 101st note (onset 1.0) and newest = onset 12.99.
- DEVIATION FROM PLAN: removed the orphaned NgramIndex/RerankFeatures *type imports* from session-brain.ts in THIS task (plan deferred to Task 4). Reason: rewriting tryIdentifyPiece orphaned them and tsconfig's noUnusedLocals (TS6196) breaks the Task-2 Step-4 "typecheck clean" gate otherwise. Build-rule compliant (remove imports YOUR change orphaned). Task 4 therefore only deletes them from wasm-bridge.ts, not session-brain.ts.
- Verified no regressions: canary, concurrency, alarm(7), unit(18), test(2), schema(6) all pass. typecheck == baseline.
- finalizeChunk (chunk_ready) wiring ships covered only via the shared helper (eval_chunk test exercises it); canary seeds pieceLocked=true so it bypasses identification. KNOWN GAP per /challenge — a future deployed-AMT verification must confirm the live chunk_ready path locks.

## Task 3 (SHA 1f1ee084)
- Added ambiguous-no-lock + sub-threshold-no-lock tests. No production change.
- FALSIFICATION PROBE (caution #3) recorded in commit body: set PIECE_ID_MARGIN_THRESHOLD = -1, the "ambiguous" test FAILED ("expected true to be false" — false lock), restored 0.0935, all 4 green. Proves the negatives are real threshold gates.
- Group A complete: certified v2 gate is live at runtime; legacy path now dead but still present (deleted in Group B).

## Task 4 (SHA 906173ab)
- Deleted ngramRecall/rerankCandidates/dtwConfirm wrappers + NgramCandidate/RerankResult/DtwConfirmResult/NgramIndex/RerankFeatures from wasm-bridge.ts; kept identifyPiece/IdentifyResult + matchPieceText/TextMatchResult/CatalogEntry. Dropped legacy mocks + ngramRecall describe blocks from node + workerd bridge tests. Grep guard: zero residual TS refs. node bridge 3 pass, workerd bridge 6 pass.

## Task 5 (SHA 035747ed)
- Deleted ngram.rs/rerank.rs/dtw_confirm.rs/real_recording_test.rs; removed module decls + 4 legacy exports from lib.rs; removed legacy types + unused HashMap import from types.rs. cargo test 26 pass (down from 41; the env-gated real_recording failure is gone).
- DEVIATION FROM PLAN: pkg/ is gitignored (.gitignore:115 `pkg/`) and was NEVER tracked (git ls-files empty). The plan's `git add ...pkg` is wrong for this repo — the WASM pkg is a build artifact regenerated by `bun run build:wasm`. I rebuilt it locally and verified (real-WASM workerd + DO piece-id tests, 10 pass; pkg has no legacy exports, keeps identify_piece) but did NOT commit it. Task 5 commit contains only Rust src.

## Task 6 (SHA 294880f3)
- Removed `--skip real_recording` filter + its stale comment from the test-piece-id recipe (file is `Justfile`, capital J). `just test-piece-id`: 26 passed.

## Final verification
- cargo test (piece-identify): 26 passed, 0 failed (was 41; legacy ngram/rerank/dtw tests + env-gated real_recording deleted).
- typecheck: == baseline (18 pre-existing errors: 11 src/harness/*, 7 unrelated test/script; 0 new from this change).
- node test:scripts: 269 passed, 4 failed = the DOCUMENTED pre-existing catalog/__catalog__ failures (catalog-codegen, validator, __catalog__ integration). Not piece-ID related.
- workerd vitest: full-batch `bun run test` hits the DOCUMENTED boot fragility (intermittent loupe/fallback-service module-resolution flake on large isolate batches), NOT real failures. Per-directory / per-file runs all green:
  - src/do: 8 files, 44 tests pass (incl. new session-brain.piece-id.test.ts).
  - src/services: 20 files, 186 tests pass (incl. wasm-bridge node + workerd).
  - src/routes: exercises (6) + practice (16) + all others pass individually; the batched "4 failed" was the runtime startup flake.
  - src/lib/errors, src/db/* pass individually.
  - src/harness: phase2 (10), prioritize-diagnoses (1) pass; src/harness/artifacts/index.test.ts has 1 PRE-EXISTING failure (ARTIFACT_NAMES drift) — this branch touches NO src/harness files (git diff confirms), so it is unrelated.
- Full legacy-surface grep across src/**/*.ts and the Rust crate src: ZERO residual references to ngramRecall/rerankCandidates/dtwConfirm/compute_rerank_features/NgramIndex/RerankFeatures/NgramCandidate/RerankResult/DtwConfirmResult/ngram_recall/rerank_candidates/dtw_confirm. Rebuilt pkg/ also has zero legacy exports and keeps identify_piece.

## Outstanding (out of scope, per request)
- #28 eval-vs-V6 reconcile and re-locking _SONNET_BASELINE on the v2 path remain credit-blocked and are NOT part of this work.
- chunk_ready (finalizeChunk) live path ships covered only via the shared helper; a future deployed-AMT verification must confirm the live path locks (the eval_chunk integration test proves the helper, which both sites call identically).
