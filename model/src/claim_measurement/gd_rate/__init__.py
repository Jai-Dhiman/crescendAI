"""G-D: first measured per-dimension faithfulness RATE (dynamics-only, whole_piece).

#101 / #67. Pairs real generator prose (baseline_v1) with its own clip's AMT bundle,
LLM-extracts dynamics whole_piece claims, verifies each against the frozen route_verdict
(tau 6.5 locked), and reports the rate + bootstrap CI + abstention histogram.
"""
